/**
 * Factory to handle normal database operations
 * @author ndkcha
 * @since 0.4.1
 * @version 0.4.1
 */

/// <reference path="../../../typings/main.d.ts" />

(function() {
    angular.module('automintApp')
        .factory('amSettings')

    SettingsFactory.$inject = ['pdbConfig'];

    function SettingsFactory(pdbConfig) {
        //  initialize blank factory and function maps
        var factory = {
            
        }
        
        return factory;
        
        //  function definitions
    }
})();